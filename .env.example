# Server
PORT=3000

# Client email — all brochure leads are sent here
CLIENT_EMAIL=Sales@nexonbuilders.com

# SMTP (Gmail, Outlook, Zoho, custom domain, etc.)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=Sales@nexonbuilders.com
